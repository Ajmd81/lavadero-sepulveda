<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.lavaderosepulveda.crm.controller.ProveedoresController"
      spacing="15" prefWidth="900" prefHeight="600">
    
    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>
    
    <!-- Cabecera -->
    <HBox spacing="15" alignment="CENTER_LEFT">
        <Label text="Gestión de Proveedores" style="-fx-font-size: 20px; -fx-font-weight: bold;"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Button text="📥 Importar" onAction="#importarProveedores" style="-fx-background-color: #2196F3; -fx-text-fill: white;"/>
        <Button text="📤 Exportar" onAction="#exportarProveedores" style="-fx-background-color: #FF9800; -fx-text-fill: white;"/>
        <Button text="+ Nuevo Proveedor" onAction="#nuevoProveedor" style="-fx-background-color: #4CAF50; -fx-text-fill: white;"/>
    </HBox>
    
    <Separator/>
    
    <!-- Barra de búsqueda y filtros -->
    <HBox spacing="15" alignment="CENTER_LEFT">
        <TextField fx:id="txtBuscar" promptText="Buscar por nombre, NIF..." prefWidth="300"/>
        <Button text="🔍 Buscar" onAction="#buscar"/>
        <Button text="Limpiar" onAction="#limpiarBusqueda"/>
        <Region HBox.hgrow="ALWAYS"/>
        <CheckBox fx:id="chkMostrarInactivos" text="Mostrar inactivos" onAction="#cargarProveedores"/>
        <Label fx:id="lblTotal" text="Total: 0 proveedores"/>
    </HBox>
    
    <!-- Tabla de proveedores -->
    <TableView fx:id="tablaProveedores" VBox.vgrow="ALWAYS">
        <columns>
            <TableColumn fx:id="colNombre" text="Nombre" prefWidth="200"/>
            <TableColumn fx:id="colNif" text="NIF/CIF" prefWidth="100"/>
            <TableColumn fx:id="colTelefono" text="Teléfono" prefWidth="100"/>
            <TableColumn fx:id="colEmail" text="Email" prefWidth="180"/>
            <TableColumn fx:id="colContacto" text="Contacto" prefWidth="120"/>
            <TableColumn fx:id="colActivo" text="Estado" prefWidth="80"/>
            <TableColumn fx:id="colAcciones" text="Acciones" prefWidth="120"/>
        </columns>
        <placeholder>
            <Label text="No hay proveedores registrados"/>
        </placeholder>
    </TableView>
    
    <!-- Barra inferior -->
    <HBox spacing="10" alignment="CENTER_RIGHT">
        <Button text="Cerrar" onAction="#cerrar" prefWidth="100"/>
    </HBox>
    
</VBox>
